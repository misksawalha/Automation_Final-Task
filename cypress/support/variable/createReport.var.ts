import genaricHelper from "../helpers/genericHelper";

export let reportName = `Report_${(genaricHelper.genaricRandomString())}`
//Report_genaricRandomString() {        return (Math.random() * 1172) * Math.random();    }